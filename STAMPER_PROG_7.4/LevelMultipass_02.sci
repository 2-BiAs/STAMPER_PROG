function listOutput = LevelMultipass_02(listInput, pgBoundary, fDOC)
    
    listInputBuffer = listInput;
    listOutput = list();
    
    bBufferNotEmpty = %T;
    
    DuperPlot(pgMultipassBoundary);

    disp("Entering loop")
    while bBufferNotEmpty

    end

endfunction
